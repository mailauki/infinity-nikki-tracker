// Shared constants for the two legal pages. Both documents are versioned by the
// same effective date so a single edit keeps them in sync — a policy that says
// it was last updated on a different day than the terms it references reads as
// stale even when nothing material changed.
export const LEGAL_EFFECTIVE_DATE = 'August 21, 2026'

// The one contact address for privacy requests, data deletion, and terms
// questions. Kept here rather than inline so it can't drift between documents.
export const LEGAL_CONTACT_EMAIL = 'julie.ux.dev@gmail.com'
